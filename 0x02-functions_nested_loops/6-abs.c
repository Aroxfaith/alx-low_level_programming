#include "main.h"

/**
 * _abs - Check for absolute value of an integer.
 * @n: The integer to be checked.
 *
 * Return: Value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
